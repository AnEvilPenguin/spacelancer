{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "characters": {
      "title": "Characters",
      "description": "Defines characters that a player can interact with",
      "type": "array",
      "items": {
        "$ref": "Npc.json"
      }
    }
  },
  "required": ["characters"]
}